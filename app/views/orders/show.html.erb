<div class="container">
  <div class="row py-4">
    <div class="col-md-3"><%= image_tag @order.garden.banner_url, width: '100%' %></div>
    <div class="col">
      <h1>Commande #<%= @order.id %> <small><span class="badge badge-primary"><%= @order.state %></span></small></h1>
      <ul class="list-unstyled">
        <li><strong>Passée le</strong> <%= @order.created_at.to_date %></li>
        <li><strong>Jardin</strong> <%= @order.garden.name %> [<%= @order.garden_sku %>]</li>
        <li><strong>TOTAL</strong> <%= humanized_money_with_symbol @order.amount %></li>
        </li>
      </ul>
      <p>Votre commande a bien été enregistrée, livraison sous <strong>2 jours ouvrés</strong></p>
    </div>
  </div>
</div>
